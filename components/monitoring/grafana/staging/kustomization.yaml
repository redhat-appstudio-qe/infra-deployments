apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - dashboards
  - https://github.com/redhat-appstudio/o11y/config/exporters/monitoring/grafana/base?ref=118d420d86df03d3e1f3d39eb8de76156f4fd53c
  - disable-leader-elect-job.yaml

images:
- name: quay.io/redhat-appstudio/o11y
  newName: quay.io/redhat-appstudio/o11y
  newTag: 118d420d86df03d3e1f3d39eb8de76156f4fd53c

patches:
  - path: grafana-resources-patch.yaml
    target:
      name: grafana-oauth
      kind: Grafana
  - path: auto-assign-role-patch.yaml
    target:
      name: grafana-oauth
      kind: Grafana
  - path: grafana-operator-subscription-patch.yaml
    target:
      name: grafana-operator
      kind: Subscription
  # Set resyncPeriod on ALL GrafanaDashboard resources
  # Reduces reconciliation frequency (default 10m → 30m)
  - target:
      kind: GrafanaDashboard
    patch: |-
      - op: add
        path: /spec/resyncPeriod
        value: "30m"
